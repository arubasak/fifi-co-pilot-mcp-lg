# Streamlit app
streamlit==1.46.0
tavily-python

# Env handling
python-dotenv==1.1.0

# LangChain ecosystem (ensure compatibility)
# --- MODIFIED: Removed specific version pins to resolve dependency conflicts ---
# This allows the installer to find the latest compatible versions.
langchain
langchain-core
langchain-openai
langchain-tavily

# LangGraph integration
langgraph
langchain-mcp-adapters

# MCP integration
mcp-use
mcp

# YAML parsing (for config files)
pyyaml

# Other dependencies
aiofiles
altair
fastapi
openai
pinecone
pinecone-plugin-assistant
pinecone-plugin-interface
requests
uvicorn
nest_asyncio
